#!/usr/bin/env bash
set -e

echo "Running pre-commit checks..."

# Navigate to project root (hooks are in .git/hooks/)
PROJECT_ROOT="$(git rev-parse --show-toplevel)"

"$PROJECT_ROOT/scripts/checks-frontend.sh"
"$PROJECT_ROOT/scripts/checks-backend.sh"
