/**
 * Recessed code sample rendered in the selected THEME's palette
 * (raw hex via CodeToken — content, not chrome).
 */
export interface CodePreviewProps {
    children: React.ReactNode;
    style?: React.CSSProperties;
}
