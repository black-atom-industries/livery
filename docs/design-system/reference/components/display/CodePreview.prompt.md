Recessed code sample colored by the displayed theme's raw palette — use `CodeToken` (same file) for syntax spans.

```jsx
<CodePreview>
  <CodeToken color="#6B5D4C">// adapters/ghostty.ts{"\n"}</CodeToken>
  <CodeToken color="#C46A5A">const</CodeToken> path = <CodeToken color="#7E93B0">expand</CodeToken>(<CodeToken color="#8FA36B">"~/.config"</CodeToken>);
</CodePreview>
```

Convention: comments = mid primary, keywords = red, functions = blue, strings = green, types = yellow.
