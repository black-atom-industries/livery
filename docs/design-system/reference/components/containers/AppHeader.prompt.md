App header bar — wordmark (brand dot as the O) + version left, uppercase context right.

```jsx
<AppHeader version="V0.3.0" context="24 THEMES · 6 COLLECTIONS · ENV DARK" />
<AppHeader version="V0.3.0" context="SETTINGS / ADAPTERS" />
```
