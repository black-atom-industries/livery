Datasheet text field — mono label above, recessed value surface, monospace input.

```jsx
<TextInput label="CONFIG_PATH" value="~/.config/ghostty/config" />
<TextInput label="THEMES_PATH" optional placeholder="optional — leave empty to skip" />
<TextInput label="MATCH_PATTERN" value="^theme = .*$" editing hint="⏎ SAVE · esc REVERT" />
```
