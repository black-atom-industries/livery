Command-line search prompt — the `»` glyph, recessed surface, block caret when typing, `n/m` counter at right.

```jsx
<Prompt placeholder="search theme names — /" />
<Prompt value="koyo" focused count="2/24" />
```

Search is name-only by convention; collection/appearance filtering belongs to Chips or the Dialog.
