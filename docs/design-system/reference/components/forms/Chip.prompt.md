Filter chip — clickable and keyboard-addressable; active state is a full contrast inversion, never a color fill.

```jsx
<Chip active>ALL</Chip>
<Chip hotkey="3">JPN</Chip>
<Chip active focused hotkey="4">TERRA</Chip>
```

Compose in a flex row with 6px gap; separate groups with a 1px vertical rule.
