Square-knob toggle (38×22, 0 radius). On = positive knob right + strong border; off = muted knob left.

```jsx
<Toggle on onChange={fn} />
<Toggle />
<Toggle on disabled />
```

Usually leads a settings row; `space` toggles the focused row.
